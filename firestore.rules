// UNUSED — storage migrated to Google Drive (appDataFolder).
// Firestore is no longer used by this application.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
