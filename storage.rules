// UNUSED — storage migrated to Google Drive (appDataFolder).
// Firebase Storage is no longer used by this application.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
